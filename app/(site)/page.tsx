
// <!-- ========== Start Section ========== -->
// A folder name within parenthesis are not considered as routes, so we use it to store the page (homepage)
// file in it, to keep the folder structures clean
// <!-- ========== End Section ========== -->

export default function Home() {
  return (
    <div className="text-green-500">
      Hello (Site)!
    </div>
  )
}
